# ===== SUPABASE =====
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Direct database connection (for Drizzle migrations)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project.supabase.co:5432/postgres

# ===== DEEPGRAM (AI Transcription) =====
# Sign up at https://deepgram.com to get an API key
DEEPGRAM_API_KEY=dg_your_api_key_here

# ===== OPENAI (GPT AI Analysis) =====
# Sign up at https://platform.openai.com to get an API key
OPENAI_API_KEY=sk-proj-your_api_key_here

# ===== RESEND (Email Service) =====
# Sign up at https://resend.com to get an API key
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM_EMAIL=notifications@audiapro.com

# ===== APP CONFIG =====
# Your production URL (used for webhooks and emails)
NEXT_PUBLIC_APP_URL=https://audiapro.com
# or for development:
# NEXT_PUBLIC_APP_URL=http://localhost:3000

NEXT_PUBLIC_APP_NAME=AudiaPro

# ===== WORKER CONFIG (For Render background worker) =====
# How often the worker polls for new jobs (in milliseconds)
WORKER_POLL_INTERVAL_MS=5000

# Maximum concurrent jobs the worker can process
WORKER_MAX_CONCURRENT_JOBS=3

# Maximum time a job can run before timing out (in milliseconds)
WORKER_JOB_TIMEOUT_MS=300000

# ===== ENCRYPTION =====
# Generate a secure 32-byte hex string for encrypting PBX credentials
# You can generate one with: openssl rand -hex 32
ENCRYPTION_KEY=your_32_byte_hex_string_here

# ===== SENTRY (Error Tracking) =====
# Sign up at https://sentry.io and create a project
SENTRY_DSN=https://your-sentry-dsn-here
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn-here

# ===== VERCEL (Auto-populated by Vercel) =====
VERCEL_URL=your-deployment-url.vercel.app

# ===== OPTIONAL: JWT Secret (if using custom JWT auth) =====
JWT_SECRET_KEY=your_jwt_secret_here

# ===== DEVELOPMENT ONLY =====
# Set to 'development' for local dev, 'production' for production
NODE_ENV=development

# ===== NOTES =====
# 1. Never commit this file with real values to version control
# 2. In production, set these as environment variables in Vercel and Render
# 3. The ENCRYPTION_KEY must be the same across frontend and worker deployments
# 4. SUPABASE_SERVICE_ROLE_KEY should only be used in secure server contexts (API routes, worker)
# 5. For local development, copy this file to .env.local and fill in your values
